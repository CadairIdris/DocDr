<UserControl x:Class="DocDr.App.Views.ClausesPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:DocDr.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:ClausesViewModel}"
             d:DesignHeight="500" d:DesignWidth="220">
    <Grid Background="{DynamicResource ChromeBrush}">
        <TreeView x:Name="Tree" ItemsSource="{Binding Roots}"
                  BorderThickness="0" Background="Transparent"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  VirtualizingPanel.IsVirtualizing="True"
                  SelectedItemChanged="Tree_SelectedItemChanged">
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                    <Setter Property="Padding" Value="2,3" />
                    <Setter Property="AutomationProperties.Name" Value="{Binding Display}" />
                </Style>
            </TreeView.ItemContainerStyle>
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type vm:ClauseNodeViewModel}" ItemsSource="{Binding Children}">
                    <Grid MinWidth="150">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Number}" VerticalAlignment="Center"
                                   FontWeight="SemiBold" Margin="0,0,6,0" />
                        <TextBlock Grid.Column="1" Text="{Binding Title}" VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis" ToolTip="{Binding Display}" />
                        <TextBlock Grid.Column="2" Text="{Binding PageLabel}" VerticalAlignment="Center"
                                   Foreground="{DynamicResource SecondaryTextBrush}" FontSize="10" Margin="6,0,2,0" />
                    </Grid>
                </HierarchicalDataTemplate>
            </TreeView.Resources>
        </TreeView>

        <TextBlock Text="Scanning for clause headings…" Foreground="{DynamicResource HintTextBrush}" FontSize="11"
                   HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10,16"
                   TextWrapping="Wrap" TextAlignment="Center"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}" />

        <TextBlock Foreground="{DynamicResource HintTextBrush}" FontSize="11"
                   HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10,16"
                   TextWrapping="Wrap" TextAlignment="Center"
                   Text="No numbered clause headings were detected. This works best on standards with dotted-decimal numbering (Eurocode, BS EN, ISO)."
                   Visibility="{Binding IsEmptyResult, Converter={StaticResource BoolToVisibility}}" />
    </Grid>
</UserControl>
